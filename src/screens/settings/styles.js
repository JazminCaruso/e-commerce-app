import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'flex-start',
  },
  settingList: {
    flexGrow: 1,
  },
  buttonLogout: {
    marginBottom: 20,
  },
  buttonLogoutTablet: {
    marginBottom: 20,
    width: 500,
  },
});
